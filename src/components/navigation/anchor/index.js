


const isValidHttpUrl = (urlString) => {
  let url;
  try {
    url = new URL(urlString);
  } catch (error) {
    return false;
  }
  return url.protocol === 'http:' || url.protocol === 'https:';
}

// defined the external link here, add the no-refer attribute



// define the link inside the website this time is the dom-router


// does it also have a method to check ?

// combine them to build


const Anchor = () => (<> </>)



